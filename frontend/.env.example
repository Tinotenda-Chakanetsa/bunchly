# Where the React dev server proxies /api calls to.
VITE_API_PROXY_TARGET=http://localhost:8000

# Public API base used by the built bundle. In Docker Compose the
# nginx in the frontend container proxies /api/* to the backend, so the
# bundle can speak to a relative URL.
VITE_API_BASE_URL=/api/v1
